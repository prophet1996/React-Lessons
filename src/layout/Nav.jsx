import React from "react";
import styled from "styled-components";

const Nav = props => <div {...props}>Nav</div>;

const StyledNav = styled(Nav)`
  background-color: #03a9f4;
  padding: 1.5em 2em;
`;
export default StyledNav;
